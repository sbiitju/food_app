class ItemInfo {
  double basePrice;
  String? itemId;
  String? itemName;
  String? itemDescription;
  var itemImage;

  ItemInfo(this.basePrice, this.itemId, this.itemName, this.itemDescription,
      this.itemImage);
}
